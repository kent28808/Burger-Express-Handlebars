
<div class="container">
  <div class="jumbotron mb-3 mt-3 text-center p-5">
    <h1 class="display-4">Eat-Da-Burger!</h1>
  </div>

  <div class="row m-0">
    <div class="col-md p-0 m-0 mb-3">
      <div class="row m-0">
        <div class="col-md border p-3 mb-3 bg-cake text-center">
          <h4 class="text-cake p-0 m-0">Burgers To Eat</h4>
          <ul>
            {{#each burgers}}
            {{#unless devoured}}
            {{> burgers/burger-block devoured=1}}
            {{/unless}}
            {{/each}}
          </ul>
        </div>

        <div class="col-md-space"></div>
        <div class="col-md m-0 p-0 mb-3">
          <div class="row m-0">
            <div class="col-md col-md border p-3 mb-3 bg-cake text-center">
              <h4 class="text-cake p-0 m-0">Burgers Eaten</h4>
              <ul>
                {{#each burgers}}
                {{#if devoured}}
                {{> burgers/burger-block devoured=0}}
                {{/if}}
                {{/each}}
              </ul>
            </div>
            
            
            <h2>Add a Burger</h2>
            <form class="create-form">

              <div class="form-group">
                <label for="ca">Burger Name:</label>
                <input type="text" id="ca" name="name">
              </div>
              <div class="jumbotron mb-2 text-center p-4">
                <label class="font-weight-bold">Add a Burger:</label>
                <textarea id="cake" rows="3"></textarea><br>
                <button type="submit">Eat Later</button>

              </div>

            </form>

  <div class="text-center">
    <a href="https://github.com/kent28808/Burger-Express-Handlebars" target="new">GitHub Repo</a>
  </div>
</div>&gt;

</body>

</html>